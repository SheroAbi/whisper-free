# uiohook-napi ships prebuilt binaries (via prebuildify) for both Node and
# Electron ABIs, so no compiler toolchain is required for `npm install`.
# Keep optional-dependency installation enabled so the hotkey enhancement
# is available when prebuilds match the platform.
optional=true
fund=false
audit=false
